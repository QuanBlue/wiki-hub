# =============================================================================
# WikiHub - local development stack
#
#   cp .env.example .env
#   docker compose up -d
#
#   Frontend  http://localhost:3000
#   API docs  http://localhost:8000/docs
#   MinIO UI  http://localhost:9001
#
# Every credential comes from .env - nothing sensitive is committed here.
# =============================================================================

name: wikihub

services:
  postgres:
    image: postgres:16.10-bookworm
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-wikihub}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-wikihub}
      # Deterministic collation keeps index ordering stable across host locales.
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT_HOST:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-wikihub} -d ${POSTGRES_DB:-wikihub}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes", "--save", "60", "1"]
    volumes:
      - redis-data:/data
    ports:
      - "${REDIS_PORT_HOST:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    restart: unless-stopped
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?MINIO_ROOT_USER is required}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
      # MinIO implements no per-bucket CORS API (PutBucketCors returns "not
      # implemented"), so browser access to presigned attachment URLs is gated
      # here, server-wide, by the same origin list the backend accepts.
      # Unset, MinIO would answer every preflight with Allow-Origin: *.
      MINIO_API_CORS_ALLOW_ORIGIN: ${WIKIHUB_CORS_ORIGINS:-http://localhost:3000}
    volumes:
      - minio-data:/data
    ports:
      - "${MINIO_PORT_HOST:-9000}:9000"
      - "${MINIO_CONSOLE_PORT_HOST:-9001}:9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  # One-shot: create the bucket the application expects, then exit.
  minio-init:
    image: minio/mc:RELEASE.2025-04-16T18-13-26Z
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      BUCKET: ${WIKIHUB_S3_BUCKET:-wikihub}
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 \"$$MINIO_ROOT_USER\" \"$$MINIO_ROOT_PASSWORD\" &&
      mc mb --ignore-existing local/$$BUCKET &&
      mc anonymous set none local/$$BUCKET &&
      echo 'bucket ready: '$$BUCKET
      "
    restart: "no"

  backend:
    build:
      context: .
      dockerfile: deploy/docker/backend.Dockerfile
    restart: unless-stopped
    env_file: [.env]
    environment:
      WIKIHUB_DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-wikihub}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-wikihub}
      WIKIHUB_REDIS_URL: redis://redis:6379/0
      WIKIHUB_S3_ENDPOINT_URL: http://minio:9000
      WIKIHUB_S3_PUBLIC_ENDPOINT_URL: http://localhost:${MINIO_PORT_HOST:-9000}
      # The UI uses the same-origin Next.js proxy. Keep this for external API
      # clients and local development only; public UI domains need no entry.
      WIKIHUB_CORS_ORIGINS: ${WIKIHUB_CORS_ORIGINS:-http://localhost:3000}
      # Page export renders the real page in a headless browser; this is how
      # the backend reaches the frontend container to do that. Intentionally
      # NOT in depends_on below - frontend already depends on backend, and
      # export simply returns a clean 503 if the frontend happens to be down.
      WIKIHUB_FRONTEND_INTERNAL_URL: http://frontend:3000
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    ports:
      - "${BACKEND_PORT_HOST:-8000}:8000"
    volumes:
      # Live reload during development; remove for a production-like run.
      - ./backend:/app
    # Chromium (page export) needs more than Docker's default 64MB /dev/shm;
    # --disable-dev-shm-usage in app/services/browser.py covers the same risk
    # from the other side.
    shm_size: "512mb"

  worker:
    build:
      context: .
      dockerfile: deploy/docker/backend.Dockerfile
    restart: unless-stopped
    command: ["arq", "app.workers.settings.WorkerSettings"]
    env_file: [.env]
    environment:
      WIKIHUB_DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-wikihub}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-wikihub}
      WIKIHUB_REDIS_URL: redis://redis:6379/0
      WIKIHUB_S3_ENDPOINT_URL: http://minio:9000
      WIKIHUB_S3_PUBLIC_ENDPOINT_URL: http://localhost:${MINIO_PORT_HOST:-9000}
      # Same reasoning as the backend service: kept in sync so moving export
      # rendering onto the worker later needs no compose change.
      WIKIHUB_FRONTEND_INTERNAL_URL: http://frontend:3000
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    volumes:
      - ./backend:/app
    shm_size: "512mb"
    healthcheck:
      # arq heartbeats into Redis; `--check` exits non-zero when that beat goes
      # stale, so this detects a wedged worker, not merely a live process.
      test: ["CMD", "arq", "app.workers.settings.WorkerSettings", "--check"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  frontend:
    build:
      context: .
      dockerfile: deploy/docker/frontend.Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000}
        API_INTERNAL_BASE_URL: http://backend:8000
        NEXT_PUBLIC_SITE_NAME: ${NEXT_PUBLIC_SITE_NAME:-WikiHub}
    restart: unless-stopped
    environment:
      # Server-side rendering talks to the API over the compose network.
      API_INTERNAL_BASE_URL: http://backend:8000
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000}
      NEXT_PUBLIC_SITE_NAME: ${NEXT_PUBLIC_SITE_NAME:-WikiHub}
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "${FRONTEND_PORT_HOST:-3000}:3000"

volumes:
  postgres-data:
  redis-data:
  minio-data:
